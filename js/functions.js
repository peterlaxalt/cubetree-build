function headKey() {
    if (document.getElementById("overlay").className = "idle") {
      document.getElementById("overlay").className = "active";
    } else if (document.getElementById("overlay").className = "active") {
      document.getElementById("overlay").className = "idle";
    }
}
